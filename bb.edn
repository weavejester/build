{:tasks
 {:requires ([babashka.fs :as fs])
  clean    (fs/delete-tree "target")
  jar      (clojure "-T:build jar")
  outdated (apply clojure "-M:outdated" *command-line-args*)
  repl     (apply clojure "-M:repl" *command-line-args*)
  test     (apply clojure "-M:test" *command-line-args*)
  uberjar  (clojure "-T:build uberjar")}}
